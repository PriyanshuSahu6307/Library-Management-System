# Library-Management-System
A Python-based Library Management System that uses JSON for data storage. It allows users to add and manage books and members, borrow and return books, track available copies, and maintain borrowing records with unique IDs and timestamps.
